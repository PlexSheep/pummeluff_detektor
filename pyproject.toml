[project]
name = "pummeluff-detektor"
version = "0.1.0"
description = "Detecct whether an image is of a jigglypuff or not"
authors = [
    {name = "Christoph J. Scherr",email = "software@cscherr.de"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
